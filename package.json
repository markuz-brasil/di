{
  "name": "tau-di",
  "version": "0.0.0",
  "description": "es6 builder",
  "main": "index.js",
  "author": {
    "name": "markuz brasil"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "6to5": ">=1.9.10",
    "6to5ify": ">=1.0.0",
    "aliasify": ">=1.4.0",
    "brfs": ">=1.2.0",
    "browser-sync": ">=1.3.0",
    "browserify": ">=5.12.0",
    "browserify-shim": ">=3.8.0",
    "del": ">=0.1.2",
    "famous-dist-generator": ">=0.2.2",
    "gulp": ">=3.8.5",
    "gulp-6to5": ">=1.0.1",
    "gulp-cached": ">=1.0.1",
    "gulp-gzip": "0.0.8",
    "gulp-jade": ">=0.7.0",
    "gulp-load-plugins": ">=0.5.3",
    "gulp-rename": ">=1.2.0",
    "gulp-size": ">=1.0.0",
    "gulp-sourcemaps": ">=1.1.1",
    "gulp-uglify": ">=1.0.1",
    "gulp-util": ">=3.0.0",
    "pm2": ">=0.12.1",
    "regenerator": ">=0.7.7",
    "require-dir": ">=0.1.0",
    "run-sequence": ">=0.3.6",
    "setimmediate": ">=1.0.2",
    "through2": ">=0.6.1",
    "vinyl-source-stream2": ">=0.1.1"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sh ./etc/build.sh assets",
    "dev": "./node_modules/.bin/pm2 --name di start ./etc/build.sh -- watch serve assets; pm2 logs di",
    "flush": "./node_modules/.bin/pm2 delete di; ./node_modules/.bin/pm2 flush",
    "postinstall": "cp ./node_modules/famous/styles.css ./src/views/famous.css  && npm run build"
  },
  "private": true
}
